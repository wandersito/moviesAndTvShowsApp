import { ImageRoutePipe } from './image-route.pipe';

describe('ImageRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new ImageRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
